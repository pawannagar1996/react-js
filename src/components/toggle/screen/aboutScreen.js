

function AboutScreen(props) {
    const {title} = props;
    return (
      <section>
        <main>
        <h3>{title}</h3>
        </main>
      </section>
    )
  }
  
  export default AboutScreen;